/**
 * Facade re-exports for sweep utilities.
 * Split into smaller modules to keep responsibilities focused.
 */

export { getEffectiveBounds } from './bounds';
export { classifyRange, getSegments } from './segments';
